model Payment {
    id String @id @default(uuid())

    status PaymentStatus @default(UNPAID)

    amount   Decimal @db.Decimal(10, 2)
    currency String  @default("BDT")

    paymentGateway        String  @default("bKash")
    merchantInvoiceNumber String // we will use appointmentId
    bkashPaymentId        String? @unique
    bkashTrxId            String?
    payerReference        String? // user email / phone number
    paidAt                String? // as the time format is not matching to prisma postgres date time

    gateWayResponse Json? // store the response from the payment gateway

    refundTrxId  String? // store the refund transaction id if any
    refundAmount Decimal? @db.Decimal(10, 2)
    refundReason String?
    refundedAt   String? // as the time format is not matching to prisma postgres date time

    projectRequestId String         @unique
    projectRequest   ProjectRequest @relation(fields: [projectRequestId], references: [id], onDelete: Cascade, onUpdate: Cascade)

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("payments")
}
